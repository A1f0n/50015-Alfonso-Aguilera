console.log("Hello world")

let array_test = [1, "stringit", ["la", "le", "li"]]

console.log(Array)

// Utilizamos un bucle for para recorrer el array
for (let i = 0; i < array_test.length; i++) {
    // Imprimimos cada elemento en la consola
    console.log(array_test[i]);
  }

  let variable_test = 2>4
  console.log(variable_test)